{
  "buildCommand": "pip install -r requirements.txt && npm install && npm run build && python manage.py collectstatic --noinput --clear",
  "outputDirectory": "staticfiles",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/wsgi.py" }
  ]
}